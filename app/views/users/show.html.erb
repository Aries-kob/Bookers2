
<main>
  <div class='container'>    
    <div class='row'>
      <div class='col-md-3'>
        <%= render 'users/user_info', user: @user %>

        <%= render 'books/books_new', book: @book %>
      </div>    

<!-- ユーザーの投稿一覧 -->
      <div class='col-md-8 offset-md-1'>
        <%= render 'books/books', books: @books, user: @user %>
      </div>
    </div>
  </div>
</main>